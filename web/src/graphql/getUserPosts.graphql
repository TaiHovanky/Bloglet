query GetUserPosts($userId: Float!) {
  getUserPosts(userId: $userId) {
    id
    title
    body
    likes {
      user {
        id
      }
    }
    comments {
      id
      comment
      likeCount
      user {
        id
        firstName
        lastName
      }
      post {
        id
      }
    }
  }
}