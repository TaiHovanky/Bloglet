query GetUserPosts($userId: Float!) {
  getUserPosts(userId: $userId) {
    id
    title
    body
    likes {
      user {
        id
      }
    }
    comments {
      id
      comment
      user {
        id
        firstName
        lastName
      }
      post {
        id
      }
    }
  }
}