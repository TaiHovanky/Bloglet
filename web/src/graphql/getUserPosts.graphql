query GetUserPosts($userId: Float!, $cursor: Float!) {
  getUserPosts(userId: $userId, cursor: $cursor) {
    id
    content
    createdAt
    likes {
      user {
        id
      }
    }
    comments {
      id
      comment
      createdAt
      likes {
        id
        user {
          id
        }
      }
      user {
        id
        firstName
        lastName
      }
      post {
        id
      }
    }
  }
}